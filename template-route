<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" />
<style>
  .card {
    margin: 10px;
    margin-bottom: 20px;
    border: 1px solid #ccc;
    border-radius: 10px;
    box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
    transition: 0.3s;
    width: 10rem;
    text-align: center;
  }

  .card:hover {
    box-shadow: 0 8px 16px 0 rgba(0, 0, 0, 0.2);
    cursor: pointer;
  }

  .card-body {
    padding: 10px;
  }

  .card-title {
    font-size: 1rem;
  }

  .card-text {
    font-size: 3rem;
  }

  .card-wide {
    width: 15rem;
    text-align: left;
  }

  .card-cancelled {
    background-color: #ff7a00;
    color: #ffffff;
  }

  .card-completed {
    background-color: #9ccb39;
    color: #ffffff;
  }
  .card-pending {
    background-color: #2e8ce2;
    color: #ffffff;
  }
</style>
<div class="container">
  <div class="d-flex justify-content-center">
    <div class="card card-wide">
      <div class="card-body">
        <h5 class="card-title">Route Info</h5>
        <p>
          <b>Route Id</b>:&nbsp;{{response.routeId}}<br /><b>Status</b>:&nbsp;{{response.routeStatus}}<br /><b>Driver</b
          >:&nbsp;{{response.routeDriverId}}<br /><b>Active</b>:&nbsp;{{response.routeActiveFlag}}
        </p>
      </div>
    </div>
    <div class="card card-completed">
      <div class="card-body">
        <h5 class="card-title">Completed</h5>
        <p class="card-text">{{response.routeStats.completedStops}}</p>
      </div>
    </div>
    <div class="card card-pending">
      <div class="card-body">
        <h5 class="card-title">Pending</h5>
        <p class="card-text">{{response.routeStats.pendingStops}}</p>
      </div>
    </div>
    <div class="card card-cancelled">
      <div class="card-body">
        <h5 class="card-title">Cancelled</h5>
        <p class="card-text">{{response.routeStats.cancelledStops}}</p>
      </div>
    </div>
    <div class="card">
      <div class="card-body">
        <h5 class="card-title">Route Optimized</h5>
        <p class="card-text">{{response.isRouteOptimized}}</p>
      </div>
    </div>
  </div>
</div>
<table class="table">
  <thead>
    <tr>
      <th>Optim Seq Num</th>
      <th>Stop ID</th>
      <th>Stop Type</th>
      <th>Stop Status</th>
      <th>Stop Basket Id</th>
      <th>Stop Address</th>
    </tr>
  </thead>
  <tbody>
    {{#each response.routeStops}}
    <tr>
      <td>{{stopOptimSeqNum}}</td>
      <td>{{stopId}}</td>
      <td>{{stopType}}</td>
      <td>{{stopStatus}}</td>
      <td>{{stopBasketId}}</td>
      <td>{{stopAddress}}</td>
    </tr>

    {{/each}}
  </tbody>
</table>
